# Plan: Add Health Check Endpoint to Listener

> Generated by: Codex
> Date: 2026-03-07
> Status: draft

## Goal

Add a `/health` command to the Telegram bot so operators can verify
the listener is alive and the gateway is reachable.

## Context

Currently the only way to check listener health is via `bob-hibernate status`
on the host. A Telegram `/health` command lets authorized users check
remotely without SSH access.

## Tasks

- [ ] Add `handle_health` function to `src/listener.py`
- [ ] Wire `/health` into the command dispatcher
- [ ] Return gateway port reachability + uptime
- [ ] Add tests for the new handler
- [ ] Update README with the new command

## Files to Change

| File | Action | Notes |
|------|--------|-------|
| `src/listener.py` | modify | Add health handler + dispatcher entry |
| `README.md` | modify | Document `/health` command |

## Security Considerations

- Health output must NOT leak tokens, secrets, or internal IPs.
- Must respect the same auth allowlist as other commands.
- Rate limiting applies to `/health` like any other command.

## Review Checklist (for Claude Code)

- [ ] Plan aligns with CLAUDE.md coding standards
- [ ] No secrets introduced
- [ ] Auth uses numeric IDs only
- [ ] Gateway stays on loopback
- [ ] Rate limiting preserved
- [ ] Changes are isolated to this plugin
